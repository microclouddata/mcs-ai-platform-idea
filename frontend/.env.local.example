# When running the frontend against the local gateway (recommended):
NEXT_PUBLIC_API_BASE=http://localhost:8080/api
# When running the frontend directly against the backend (no gateway):
# NEXT_PUBLIC_API_BASE=http://localhost:8081/api
