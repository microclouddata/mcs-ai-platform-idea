apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: mcs-ai-platform
  labels:
    app: rag-service
spec:
  selector:
    app: rag-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
  type: ClusterIP
