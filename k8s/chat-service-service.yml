apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: mcs-ai-platform
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
  type: ClusterIP
