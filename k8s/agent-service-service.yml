apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: mcs-ai-platform
  labels:
    app: agent-service
spec:
  selector:
    app: agent-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
